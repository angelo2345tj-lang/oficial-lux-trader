/**
 * @deprecated Removido — use computeOrganicConfidence() em institutionalScoreEngine.ts.
 * Mantido apenas para evitar imports quebrados em código legado.
 */
export function applyInstitutionalScoreCap(score: number): number {
  return score;
}
