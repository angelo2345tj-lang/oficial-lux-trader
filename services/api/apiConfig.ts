/** @deprecated Import from `src/config/api` — re-export para compatibilidade. */
export {
  API_BASE,
  API_ORIGIN,
  BASE_API_URL,
  HEALTH_URL,
  endpoints,
  apiFetch,
  parseApiError,
  resolveApiOrigin,
  resolveApiV1Base,
  resolveApiWebSocketUrl,
  resolveBinanceWsUrl,
  isCrossOriginApi,
} from '../../src/config/api';

export { default } from '../../src/config/api';
